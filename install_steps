#BSPWM
apt install bspwm

rm rf ~/.config/bspwm/
cp -r ./bspwm/ ~/.config

#SXHKD
apt intall sxhkd

rm rf ~/.config/sxhkd/
cp -r ./sxhkd/ ~/.config

#FEH
apt install feh

#PICOM
# Como puedas con el github de picom para la latest release
#apt install picom
cp -r ./picom ~/.config

#POLYBAR
apt install polybar
git clone https://github.com/adi1090x/polybar-themes.git
chmod 777 setup.sh
./setup.sh
Simple
cp -r ./polybar/ ~/.config

#ROFI
apt install rofi

#ZSH
apt install zsh
cp -r ./zsh/ ~/.config

#NVIM
# Con el github de neovim luego lazy vim y por último copia pega del directorio
cp -r /.nvim ~/.config

#KITTY
# Instalar desde su p'agina
cp -r kitty ~/.config/

# Hack Nerd Fonts
cp -r fonts /usr/local/share/

#Install batcat
apt install bat

#Install LSD 
apt install lsd

#Powerlevel10k
# Cambiar el usuario dentro de .zshrc 
cp ./Powerlevel10k ~/

#
# Plugins de zsh
sudo apt install zsh-autocomplete zsh-autosuggestions zsh-syntax-highlighting
# Para el sudo hay que crear un directorio o copiar y pegar el que est'a en /usr/share/
# Ahora hay que cambiar el color del autosuggesrions donde pone fg=8 cambiarlo a otro color fg=214 por ejemplo
wget https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/refs/heads/master/plugins/sudo/sudo.plugin.zsh


