#BSPWM
apt install bspwm

rm rf ~/.config/bspwm/
cp -r ./bspwm/ ~/.config

#SXHKD
apt intall sxhkd

rm rf ~/.config/sxhkd/
cp -r ./sxhkd/ ~/.config

#FEH
apt install feh

#PICOM
# Como puedas con el github de picom
#apt install picom
cp -r ./picom ~/.config

#POLYBAR
apt install polybar
cp -r ./polybar/ ~/.config

#ROFI
apt install rofi

#ZSH
apt install zsh
cp -r ./zsh/ ~/.config

#NVIM
# Con el github de neovim luego lazy vim y por último copia pega del directorio

#KITTY
apt install kitty
cp -r ./kitty/ ~/.config
